---
kind: pipeline
type: docker
name: production

steps:
- name: build
  image: alpine
  volumes:
  - name: mura.io
    path: /mura.io
  commands:
  - ls -lha

volumes:
- name: mura.io
  host:
    path: /var/www/mura.io
...