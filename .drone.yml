---
kind: pipeline
type: docker
name: production

steps:
- name: build
  image: alpine
  volumes:
  - name: mura
    path: /mura
  commands:
  - cd mura.io
  - ls

volumes:
- name: mura
  host:
    path: /var/www/mura.io
...